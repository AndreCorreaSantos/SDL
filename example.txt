#in vec3 pos, out func SignedDistance, out func Color,

func sqrt(float x){
    return x**0.5
}

func length(vec3 v){
    return sqrt(v.x*v.x + v.y*v.y + v.z*v.z)
}

func SignedDistance(vec3 pos){
    local center = vec3(0.0,0.0,0.0)
    local radius = 5.0
    return length(p - center) - radius
}

func Color(vec3 pos){
    local r = 1.0f
    local g = 1.0f
    local b
    local a
    b = pos.x
    a = pos.y
    return vec4(r,g,b,a)
}


